"""Lure Mold Generator internals.

Packaged rather than flat: Fusion add-ins share one Python namespace, so a
bare module called "layout" or "orient" would collide with any other add-in
that has one.
"""
